# Phase 1 — Data Infrastructure
gitpython
PyGithub
httpx
tenacity
sqlmodel
pydantic-settings
python-dotenv

# Phase 2 — Parsing, Entities & Chunking
unidiff                     # structured diff parsing
tree-sitter>=0.21           # AST-based code parsing
tree-sitter-python          # Python grammar for tree-sitter
spacy>=3.7                  # NER entity extraction (run: python -m spacy download en_core_web_sm)
rapidfuzz                   # fuzzy entity alias resolution
markdownify                 # HTML → Markdown conversion
beautifulsoup4              # HTML parsing fallback
keybert                     # unsupervised keyphrase extraction
tiktoken                    # token counting for context assembly

# Phase 3 — Embedding & Indexing
qdrant-client>=1.9          # local-mode vector store with named vectors
bm25s                       # pure-Python BM25 sparse index
PyStemmer                   # stemmer backend for bm25s
diskcache                   # persistent embedding cache

# Phase 5 — Generation
google-genai
jinja2
pydantic

# Phase 6 — Evaluation
ragas
mlflow

# Phase 7 — Interface
streamlit
typer

# Optional — Reranker
sentence-transformers

# Dev / Testing
pytest
pytest-asyncio
